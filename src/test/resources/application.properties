# Shared API keys for @SpringBootTest acceptance tests. Business-logic tests send
# the USER key; the authentication tests assert behaviour for each role.
api.keys[0].key=test-user-key
api.keys[0].role=USER
api.keys[1].key=test-admin-key
api.keys[1].role=ADMIN
